server {
	listen 80;
  server_name tour.aeeternity.com;
	root /workspace/tour;
	index index.html;

  location ~ .*\.(gif|jpg|jpeg|png|bmp|swf)$
  {
    root /workspace/tour;
  }

  location ~ .*\.(js|html|css)?$
  {
    root /workspace/tour;
    expires 30d;
  }
}
